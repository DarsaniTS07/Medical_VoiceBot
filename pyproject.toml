[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-doctor-voicebot"
version = "0.1.0"
description = "AI Medical Voice Bot with Vision LLM"

[tool.vercel]
entrypoint = "gradio_app.py"
